spring:
  # MySQL 5.6 设置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/bbs_db?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: root
    # password: asiayak
    druid:
      initial-size: 0
      min-idle: 0
      max-active: 20
      test-on-borrow: false
      stat-view-servlet:
        allow: true

# 图片服务器的配置信息
storage:
  #  path: ${FILE_UPLOAD_PATH:/Users/walker/Projects/BBSForum/bbs/upload/}
  path: D:\temp\temp\file20260228\upload\