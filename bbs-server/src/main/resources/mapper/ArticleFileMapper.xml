<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.walker.mapper.ArticleFileMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.walker.pojo.ArticleFile">
        <id column="file_id" property="fileId" />
        <result column="file_name" property="fileName" />
        <result column="file_type" property="fileType" />
        <result column="file_path" property="filePath" />
        <result column="article_id" property="articleId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        file_id,file_name,file_type,file_path,article_id
    </sql>

    <!--给文章和附件添加绑定关系-->
    <update id="updateArticleFile">
        UPDATE bbs_article_file
        SET article_id = #{articleId,jdbcType=INTEGER}
        WHERE file_id IN
        <foreach collection="fileIds" item="fileId" open="(" separator="," close=")">
            #{fileId,jdbcType=INTEGER}
        </foreach>
    </update>

    <!--解绑文章绑定的附件-->
    <update id="unBindArticleFile">
        UPDATE bbs_article_file
        SET article_id = NULL
        WHERE article_id = #{articleId,jdbcType=INTEGER}
    </update>

</mapper>
